# ==============================================
# Patent Expiration API - Environment Variables
# ==============================================

# Application
APP_NAME="Patent Expiration API"
APP_VERSION="1.0.0"
DEBUG=false
ENVIRONMENT=production
LOG_LEVEL=INFO

# API Server
API_HOST=0.0.0.0
API_PORT=8000

# Database (PostgreSQL)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://patent_user:your_secure_password@localhost:5432/patent_db

# EPO OPS API (European Patent Office)
# Get credentials at: https://developers.epo.org/
EPO_CONSUMER_KEY=your_epo_consumer_key_here
EPO_CONSUMER_SECRET=your_epo_consumer_secret_here
EPO_BASE_URL=https://ops.epo.org/3.2

# USPTO PatentsView API (US Patent Office)
# Get API key at: https://search.patentsview.org/
USPTO_API_KEY=your_uspto_api_key_here
USPTO_BASE_URL=https://search.patentsview.org/api/v1

# Lens.org Patent API (Optional - currently disabled)
LENS_API_TOKEN=your_lens_token_here
LENS_BASE_URL=https://api.lens.org/patent/search
LENS_RATE_LIMIT_PER_MINUTE=10

# Cache Settings
CACHE_TTL_DAYS=30
CACHE_REFRESH_TOP_N=100

# Rate Limiting (requests per month)
RATE_LIMIT_FREE=20
RATE_LIMIT_BASIC=1000
RATE_LIMIT_PRO=10000

# RapidAPI Integration (Optional)
RAPIDAPI_PROXY_SECRET=your_rapidapi_secret_here
RAPIDAPI_HEADER_NAME=X-RapidAPI-Proxy-Secret

# Security
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your_very_secret_random_key_here_min_32_characters

# CORS (allowed origins)
ALLOWED_HOSTS=*
CORS_ORIGINS=["*"]

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# External Services
REQUEST_TIMEOUT=30
MAX_RETRIES=3

# ==============================================
# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your real API keys
# 3. Generate SECRET_KEY: python -c "import secrets; print(secrets.token_urlsafe(32))"
# 4. Never commit .env file to Git!
# ==============================================